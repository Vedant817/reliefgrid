# Convex
VITE_CONVEX_URL=

# OpenAI is the primary extraction/clarification provider — keep values out of git
OPENAI_API_KEY=
OPENAI_MODEL=

# Groq (GPT-OSS) fallback when OpenAI is missing or the request fails
GROQ_API_KEY=
GROQ_MODEL=

# Firecrawl — keep values out of git
FIRECRAWL_API_KEY=

# Exa fallback for web search/content when Firecrawl is unavailable or out of credits
EXA_API_KEY=

# AgentMail — keep values out of git
AGENTMAIL_API_KEY=
AGENTMAIL_INBOX=
AGENTMAIL_WEBHOOK_SECRET=
