# Convex
VITE_CONVEX_URL=

# OpenAI (via Convex AI Gateway or direct) — keep values out of git
OPENAI_API_KEY=
OPENAI_MODEL=

# Groq free lane (OpenAI-compatible) — keep values out of git
GROQ_API_KEY=
GROQ_MODEL=

# Firecrawl — keep values out of git
FIRECRAWL_API_KEY=

# AgentMail — keep values out of git
AGENTMAIL_API_KEY=
AGENTMAIL_WEBHOOK_SECRET=
